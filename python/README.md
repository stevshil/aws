# Launching VMs using the python code

```
cvm <region> <vpcid> <provisionFile>
```

## Provision files;

* nginx
  * Installs a simple NGINX server
* jenkins.sh
  * Installs a Jenkins server with username/password = admin/admin
