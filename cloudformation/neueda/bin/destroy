#!/bin/bash

if [[ $1 == 'all' ]]
then
	# Delete everything
	for x in repo itrs git elk docker db teamcity ebs vpc
	do
		aws cloudformation delete-stack --stack-name $x
	done
else
	# Delete specific stack
	aws cloudformation delete-stack --stack-name $1
fi
